#include "HalfSelectionSort.hpp"

int halfSelectionSort ( std::vector<int>& nums, int& duration)
{
	int median = 0;
	for(std::vector::iterator it = nums.begin(); it < nums.end() / 2; ++it)
	{
		
	}
}
